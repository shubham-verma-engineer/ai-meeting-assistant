server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:meetingdb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

openai:
  apiKey: ${OPENAI_API_KEY:}
whisper:
  url: http://whisper-service:5000/transcribe
